server:
  port: 8888
spring:
  application:
    name: config-service
  cloud:
    config:
      server:
        git:
          default-label: 06-multi-instances
          uri: https://github.com/joosbeen/microservices-luigi.git
          search-paths: config-data
